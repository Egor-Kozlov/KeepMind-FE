import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const CommonButton = ({Icon, title}) => {
  return (
    <View>
      <Text>Play</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
